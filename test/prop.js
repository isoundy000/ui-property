describe('<editor-prop>', function() {
    var propEL;
    beforeEach(function ( done ) {
        fixture('widget', function ( el ) {
            propEL = el;
            done();
        });
    });
});
